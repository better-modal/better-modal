{
    "name": "root",
    "private": true,
    "engines": {
        "node": "^20",
        "pnpm": "^10.4.1"
    },
    "packageManager": "pnpm@10.4.1",
    "scripts": {
        "dev": "pnpm --filter next dev",
        "version": "changeset version",
        "cs": "changeset",
        "release": "pnpm --filter better-modal build && changeset publish",
        "lint": "pnpm --filter better-modal lint",
        "build": "pnpm --filter better-modal build"
    },
    "devDependencies": {
        "size-limit": "^11.2.0",
        "@biomejs/biome": "2.2.0",
        "@changesets/cli": "^2.27.9"
    },
    "overrides": {
        "better-modal": "workspace:*"
    }
}